staticSites:
  - name: fe-unistore
    buildCommand: npm run build
    publishDir: build
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
    headers:
      - path: "/*"
        name: "Cache-Control"
        value: "no-cache, no-store, must-revalidate"